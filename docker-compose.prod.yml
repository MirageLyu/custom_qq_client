services:
  openclaw:
    build: .
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./openclaw-data:/home/node/.openclaw
      - ./config.toml:/app/config.toml
      - ./data:/app/data
    env_file:
      - ./openclaw-config/.env
